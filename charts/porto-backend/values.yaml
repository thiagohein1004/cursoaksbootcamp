name: porto-backend
image:
  tag: v1.0.0
  name: porto_backend
  registry: maratonaaks.azurecr.io
appPort: 3000

deployment:
  envs:
    dbName: ship_manager
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi

ingress:
  hostName: porto-backend.67adafcf87fe4e9abac2.eastus.aksapp.io

service:
  port:

secret:
  name:
  mongodb_uri: mongodb+srv://root:SCvQd6jrSQgrwVu0@maratona-aks.48pkj.azure.mongodb.net/ship_manager?retryWrites=true&w=majority

hpa:
  minReplicas: 1
  maxReplicas: 8
  CPUUtilization: 20
  memoryUtilization: 35
